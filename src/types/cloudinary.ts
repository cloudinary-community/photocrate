export interface CloudinaryResource {
  asset_id: string;
  bytes: number;
  created_at: string;
  format: string;
  height: number;
  public_id: string;
  resource_type: string;
  secure_url: string;
  tags: Array<string>;
  width: number;
}